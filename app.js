var str = prompt("ingresa la palabra a encriptar") // infreso str
function encrypt (str, num) {  // funcion para encriptar str
	for (var i = 0; i < str.length; i++) {
		str[i]
		if (srt.charCodeAt(0) >= 65 && str.charCodeAt(0) <= 90) { // devuelve el valor del unicode dela letra ingresada
			 (x - 65 + x) % 65 + 65;
		}if else {
			str.charCodeAt >= 97 && str.charCodeAe <= 122
		}
	}
	}


}